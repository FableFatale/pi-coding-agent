Metadata-Version: 2.4
Name: pi-memory
Version: 1.0.0
Summary: 三层记忆系统 - L1 Working, L2 Episodic, L3 Semantic
Author: Pi Team
License: MIT
Project-URL: Homepage, https://github.com/pi-team/pi-memory
Keywords: memory,ai,redis,postgresql,pinecone
Classifier: Development Status :: 4 - Beta
Classifier: Intended Audience :: Developers
Classifier: License :: OSI Approved :: MIT License
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.8
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Requires-Python: >=3.8
Description-Content-Type: text/markdown
Requires-Dist: redis>=4.5.0
Requires-Dist: psycopg2-binary>=2.9.0
Provides-Extra: pinecone
Requires-Dist: pinecone-client>=3.0.0; extra == "pinecone"
Provides-Extra: dev
Requires-Dist: pytest>=7.0.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.21.0; extra == "dev"
Requires-Dist: black>=23.0.0; extra == "dev"
Provides-Extra: all
Requires-Dist: pinecone-client>=3.0.0; extra == "all"
Requires-Dist: pytest>=7.0.0; extra == "all"
Requires-Dist: pytest-asyncio>=0.21.0; extra == "all"
Requires-Dist: black>=23.0.0; extra == "all"
Dynamic: requires-python

# Pi Memory - 三层记忆系统

## 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│  L1 Working Memory (Redis)                                  │
│  - TTL=1小时，访问自动延长                                   │
│  - 短期会话信息                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  L2 Episodic Memory (PostgreSQL + pgvector)                 │
│  - 对话历史片段                                             │
│  - FTS全文 + 向量混合搜索                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  L3 Semantic Memory (Pinecone) - 可选                       │
│  - 技能向量存储                                             │
│  - 冲突检测                                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 快速开始

### 1. 安装 Redis

下载: https://redis.io/docs/getting-started/

或使用 Docker:
```bash
docker run -d -p 6379:6379 redis
```

### 2. 安装依赖

```bash
cd D:\pi-coding-agent\pi-memory
pip install -e .
```

### 3. 初始化数据库

双击运行:
```
init_db.bat
```

### 4. 修改配置

编辑 `.env` 文件，修改 PostgreSQL 密码:
```
L2_PG_PASSWORD=您的密码
```

### 5. 运行示例

双击:
```
run_example.bat
```

---

## 使用示例

```python
from pi_memory import MemorySystem

memory = MemorySystem()

# L1: 短期会话 (TTL=1小时，访问自动延长)
memory.session_set("user:123", {"task": "写代码", "context": "Python"})
data = memory.session_get("user:123")  # 自动延长TTL

# L2: 情景记忆 (FTS + 向量搜索)
memory.remember("用户张三询问Python优化", metadata={"user": "张三"})
results = memory.recall("张三 Python")

# L3: 技能存储 + 冲突检测 (可选)
memory.learn_skill("python-fastapi", name="FastAPI", vector=[...])
conflicts = memory.check_conflict(vector=[...])
```

---

## 依赖说明

| 组件 | 状态 | 说明 |
|------|------|------|
| Redis | 需要安装 | L1工作记忆 |
| PostgreSQL | 需要启用pgvector | L2情景记忆 |
| Pinecone | 可选 | L3语义记忆 |

---

## 快捷脚本

| 脚本 | 说明 |
|------|------|
| `init_db.bat` | 初始化数据库 |
| `run_example.bat` | 运行示例 |
